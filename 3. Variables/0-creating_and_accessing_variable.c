#include <stdio.h>

int main() {

    // Creating an integer variable
    int n;

    // Initializinng the created variable
    n = 3;

    // Accessing the value stored in the variable
    printf("%d", n);

    return 0;
}

