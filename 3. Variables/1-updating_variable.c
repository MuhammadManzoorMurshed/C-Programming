#include <stdio.h>

int main() {

    // Creating an integer variable
    int n = 3;

    // Changing the stored data
    n = 22;

    // Accessing the value stored in the variable
    printf("%d", n);

    return 0;
}
